import React from 'react';
import DashboardLayout from "@/Layouts/DashboardLayout/DashboardLayout";

const Events = () => {
    return (
        <div>

        </div>
    );
};

Events.layout = (page) => <DashboardLayout title={"Events"} children={page}/>

export default Events;
