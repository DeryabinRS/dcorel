import React from 'react';
import DashboardLayout from "@/Layouts/DashboardLayout/DashboardLayout";

const EventTypes = () => {
    return (
        <div>

        </div>
    );
};

EventTypes.layout = (page) => <DashboardLayout title={"EventTypes"} children={page}/>

export default EventTypes;
